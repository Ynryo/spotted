<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/flTimelineLast"
    android:layout_width="20dp"
    android:layout_height="match_parent">

    <View
        android:id="@+id/vLineTop"
        android:layout_width="20dp"
        android:layout_height="0dp"
        android:layout_marginBottom="-2dp"
        android:background="@drawable/timeline_bar_bottom_round"
        app:layout_constraintBottom_toBottomOf="@+id/vStopDot"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/vStopDot"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:background="@drawable/timeline_dot_solid"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>